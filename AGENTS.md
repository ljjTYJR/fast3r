# AGENTS.md

## Scope
- Repository: fast3r (this folder only).
- Default working directory: `/home/shuo/projects/shuo/paper/cvpr_26/methods/fast3r`.

## Guidance
- Prefer minimal, targeted changes.
- Do not run networked commands unless explicitly requested.
- Avoid modifying large data or checkpoint files unless asked.
- When creating git commits, sign with the email `shuo.sun@oru.se`.

## Common commands
- `rg --files` for file discovery.
- `rg <pattern>` for text search.
- `git status --short` to review working tree.

## Tests
- No default test command is defined; ask before running tests.
